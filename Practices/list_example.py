list = [10, 12, 13, 14, 15, 18, 15]
# list.append :- add an item to the end of the list
# list.append(16)
# list.extend :- Extend the list by appending all the item from another list which we want to extend with first list
# a=['r','o','n','i','t']
# list.extend(a)
# list.insert :-Insert an item at a given position
# list.insert(1,11)
# list.remove(r):- remove the first item from the list whose value is equal to r.
# list.remove()
# list.pop:- list.pop() in this code remove the last item from the list but list.pop(2) in this code remove the item at the given position mean 13 was removed
# list.pop(2)
# list.clear() #clear all the list
# del list[:] #clear all the list
# print(list.count(15)) #return the number of times x appears in the list
# list.sort() #sorting the list
# list.reverse() #reverse the list
# pending - list.copy() and list.index(x[,start[,end]])-index for searching
print(list)
